schema_version = "1.0.0"

id = "hw_mode_highlight"
version = "0.2.0"
name = "hw_modeHighlight"
tagline = "Highlight the 3D View header based on mesh select mode"
maintainer = "Hal Wayland"
type = "add-on"

blender_version_min = "5.1.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

tags = ["User Interface"]
